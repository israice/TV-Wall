services:
  app:
    build: .
    container_name: livestream-tv-wall
    restart: unless-stopped
    ports:
      - "80:5013"
    volumes:
      - ./DATA:/app/DATA
    environment:
      - PYTHONUNBUFFERED=1
      - APP_HOST=0.0.0.0
      - DEV_MODE=0
